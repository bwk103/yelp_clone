- if @restaurants.any?
  - @restaurants.each do |restaurant|
    %h2= link_to restaurant.name, restaurant_path(restaurant)
    %h3= restaurant.reviews.first.rating if restaurant.reviews.any?
    %h3= restaurant.reviews.first.thoughts if restaurant.reviews.any?
    = link_to "Review #{restaurant.name}", new_restaurant_review_path(restaurant)
    = link_to "Edit #{restaurant.name}", edit_restaurant_path(restaurant)
    = link_to "Delete #{restaurant.name}", restaurant_path(restaurant), method: :delete
- else
  No restaurants yet

%br
%br
%a(href=new_restaurant_url) Add a restaurant
